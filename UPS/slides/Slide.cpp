#include "Slide.h"

/*
void UPS::Slide::start()
{
    timeFromStart = Time::now();
}

void UPS::Slide::play(TimeTypeSec t)
{
    for (auto obj : objects){
        Primitive::get(obj.first)->draw(t,obj.second);
    }
}

void UPS::Slide::intro(parameter t)
{
    for (auto obj : objects)
        Primitive::get(obj.first)->intro(t,obj.second);
}

void UPS::Slide::outro(parameter t)
{
    for (auto obj : objects)
        Primitive::get(obj.first)->outro(t,obj.second);
}

*/
