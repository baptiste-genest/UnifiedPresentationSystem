#include "Plot.h"

/*

slope::Plot::Plot(const scalars& v)
{
    values.resize(v.size());
    for (size_t i = 0;i<v.size();i++)
        values[i] = static_cast<float>(v[i]);
}

void slope::Plot::draw(const TimeObject &time, const StateInSlide &sis)
{
    ImGui::PlotLines("test",values.data(),values.size());
}

void slope::Plot::intro(parameter t, const StateInSlide &sis)
{

}

void slope::Plot::outro(parameter t, const StateInSlide &sis)
{

}

slope::Primitive::Size slope::Plot::getSize() const
{

}

*/
