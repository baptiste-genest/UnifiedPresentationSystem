#include "Plot.h"

/*

UPS::Plot::Plot(const scalars& v)
{
    values.resize(v.size());
    for (size_t i = 0;i<v.size();i++)
        values[i] = static_cast<float>(v[i]);
}

void UPS::Plot::draw(const TimeObject &time, const StateInSlide &sis)
{
    ImGui::PlotLines("test",values.data(),values.size());
}

void UPS::Plot::intro(parameter t, const StateInSlide &sis)
{

}

void UPS::Plot::outro(parameter t, const StateInSlide &sis)
{

}

UPS::Primitive::Size UPS::Plot::getSize() const
{

}

*/
